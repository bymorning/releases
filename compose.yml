services:
  app:
    image: ghcr.io/bymorning/gateway:latest
    ports:
      - "127.0.0.1:3210:3210"
    environment:
      APP_ORIGIN: http://localhost:3210
      PORT: "3210"
      HOST: "0.0.0.0"
      BYMORNING_DATA_DIR: /data
    volumes:
      - bymorning-gateway-data:/data
    restart: unless-stopped

volumes:
  bymorning-gateway-data:
